# Optional: absolute API URL (otherwise Vite proxies /api to Django)
# VITE_API_BASE=http://127.0.0.1:8000

# Optional: embed token for local dev (otherwise paste in sidebar)
# VITE_AUTH_TOKEN=your-drf-token
